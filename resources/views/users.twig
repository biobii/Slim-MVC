<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Data Users</title>
  </head>
  <body>

    <h2>List:</h2>

    {% for user in users %}
      <li>{{ user.name }}, from {{ user.address }}</li>
    {% endfor %}

  </body>
</html>
